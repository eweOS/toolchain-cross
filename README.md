# Cross-compile toolchain generator for eweOS

## Usage:

environment files:

- aarch64.env
- riscv64.env

Execute `{0-6}_*.sh`. eweOS is recommended.

## TODO

`tar` from busybox can not decompress xz format.
